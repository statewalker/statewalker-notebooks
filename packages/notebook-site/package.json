{
  "name": "@statewalker/notebook-site",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Compose a SiteHandler serving a built notebook site: pages, module dependencies and the rebuild event stream.",
  "license": "MIT",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsdown",
    "prepack": "pnpm run build",
    "test": "pnpm run build && vitest run --passWithNoTests",
    "test:browser": "vitest run test-browser --config vitest.browser.config.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test-browser.json"
  },
  "dependencies": {
    "@statewalker/notebook-events": "workspace:*"
  },
  "peerDependencies": {
    "@statewalker/webrun-files": "workspace:*",
    "@statewalker/webrun-site-builder": "workspace:*"
  },
  "devDependencies": {
    "@statewalker/webrun-files-mem": "workspace:*",
    "@statewalker/webrun-http-browser": "^0.6.0",
    "@statewalker/webrun-site-host": "^0.1.7",
    "esbuild": "^0.28.2",
    "playwright": "^1.63.0",
    "tsdown": "^0.23.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  }
}
